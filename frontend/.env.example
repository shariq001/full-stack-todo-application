# =============================================================================
# Next.js Frontend - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# Note: NEXT_PUBLIC_* variables are exposed to the browser
# Do NOT include secrets in NEXT_PUBLIC_* variables
#

# API Base URL - Where your FastAPI backend is running
# For local development:
#   NEXT_PUBLIC_API_BASE_URL="http://localhost:8000"
# For production/deployed backend:
#   NEXT_PUBLIC_API_BASE_URL="https://your-deployed-backend.com"
# For HuggingFace Spaces:
#   NEXT_PUBLIC_API_BASE_URL="https://username-project.hf.space"
NEXT_PUBLIC_API_BASE_URL="http://localhost:8000"

# Better Auth Frontend URL
# This should be your frontend URL for authentication flow
# For local development:
#   NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"
# For production:
#   NEXT_PUBLIC_BETTER_AUTH_URL="https://your-domain.com"
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# Database Connection String (from root .env)
# Used during build time by some configurations
# Same as in root .env
DATABASE_URL="postgresql://user:password@localhost:5432/todo_app"

# Authentication Secret (from root .env)
# Used for JWT token verification
# Keep this secret - never expose in browser!
BETTER_AUTH_SECRET="your_random_secret_key_minimum_32_characters_long"